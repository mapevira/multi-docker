sudo: required
services:
  - docker

before_install:
  - docker build -t mapevira/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run mapevira/react-test npm test -- --coverage

after_success:
  - docker build -t mapevira/multi-client ./client
  - docker build -t mapevira/multi-nginx ./nginx
  - docker build -t mapevira/multi-server ./server
  - docker build -t mapevira/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push mapevira/multi-client
  - docker push mapevira/multi-nginx
  - docker push mapevira/multi-server
  - docker push mapevira/multi-worker
  